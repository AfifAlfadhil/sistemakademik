# nixpacks.toml untuk konfigurasi deployment Railway
# Menggunakan aptPkgs untuk menginstal Tesseract OCR dan model bahasa Indonesia secara native.

[providers]
python = {}

[phases.setup]
aptPkgs = ["tesseract-ocr", "tesseract-ocr-ind"]
